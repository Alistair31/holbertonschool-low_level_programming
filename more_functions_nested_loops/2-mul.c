#include "main.h"

/**
 * mul - Entry point
 * @a: variable that contain a number
 * @b: variable that contain another number
 * Return: result of the function
 */
int mul(int a, int b)

{
	return (a * b);
}
